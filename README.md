# 📅 Smart Day Planner (Weather-Adaptive System)
Smart Day Planner — це інтелектуальна система планування дня, яка автоматично адаптує список активностей користувача залежно від поточних погодних умов у вибраному місті. Проєкт побудований на базі мікросервісної архітектури з використанням асинхронного фреймворку FastAPI та бази даних MongoDB.

## 🏗️ Архітектура та патерни проектування
Проєкт реалізований із дотриманням принципів чистої архітектури та використанням ключових патернів проектування:

- **Strategy (Стратегія):** Основний патерн, що дозволяє динамічно змінювати алгоритм формування плану дня. Кожна погодна умова (Sunny, Rainy, Snowy, Cloudy) має власну реалізацію логіки вибору завдань.

- **Observer (Спостерігач):** Реалізований через WeatherStation, яка відстежує стан погоди та сповіщає систему про зміни для актуалізації планів.

- **Facade / Service:** Клас DayPlanner виступає єдиною точкою входу для генерації планів, приховуючи складність взаємодії між API погоди, стратегіями та базою даних.

- **Singleton/Configuration:** Централізоване керування налаштуваннями через об'єкт settings.

## 🛠️ Технологічний стек
- **Мова:** Python 3.11.

- **Фреймворк:** FastAPI.

- **База даних:** MongoDB (основна).

- **Контейнеризація:** Docker, Docker Compose.

- **Тестування:** Pytest.

## 🚀 Запуск проєкту через Docker
Це найшвидший спосіб запустити систему разом із базою даних:

```bash
# Запустіть контейнери:
docker-compose up --build

#Відкрийте додаток:
Dashboard: http://localhost:8000
Interactive API Docs (Swagger): http://localhost:8000/docs

# Зупинити контейнер
docker-compose down
```

## 🛡️ Надійність: Система Fallback Storage
Однією з ключових особливостей є механізм відмовостійкості бази даних. Якщо підключення до MongoDB відсутнє або драйвер motor не встановлено, система автоматично перемикається на Fallback JSON Storage:
- Дані зберігаються локально у файлі fallback_storage.json.
- Додаток продовжує працювати без втрати функціональності.
- При кожному запуску система логує поточний статус сховища.

## ⛈️ Робота з погодою (Mock API)
Для потреб розробки та тестування реалізовано WeatherAPI, який симулює реалістичні погодні дані на основі поточної пори року (враховуються місяці, ймовірність опадів та температурні коливання). Це дозволяє перевіряти роботу всіх стратегій без необхідності підключення платного API-ключа.

## 🧪 Тестування
Проєкт покритий тестами для перевірки логіки стратегій, API-ендпоінтів та роботи з моделями даних.
```bash
# Запуск тестів
pytest tests/
```
